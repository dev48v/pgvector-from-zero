# Copy to .env. The docker-compose Postgres below matches these defaults.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/pgvector_demo
PORT=3000

# How many Wikipedia articles to pull + embed when you run `npm run seed`.
SEED_LIMIT=300
